import React from "react";

const Version = () => {
  const version = "2.0.0"; // Replace with your actual version information

  return (
    <div>
      <h1>Version</h1>
      <p>{version}</p>
      <p>added openAI and Replicate!!</p>
      <p>added Multiple Replicate Models in LLMs</p>
    </div>
  );
};

export default Version;
